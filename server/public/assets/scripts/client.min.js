function init(){scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,1,1e3),camera.position.z=1e3,geometry=new THREE.BoxGeometry(100,100,100),material=new THREE.MeshBasicMaterial({color:16711680}),mesh=new THREE.Mesh(geometry,material),scene.add(mesh),renderer=new THREE.WebGLRenderer,renderer.setSize(800,450),document.body.appendChild(renderer.domElement)}function animate(){requestAnimationFrame(animate),mesh.rotation.x+=.01,mesh.rotation.y+=.02,renderer.render(scene,camera)}var scene,camera,renderer,geometry,material,mesh;$(document).ready(function(){init(),animate()});